name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  bun:
    uses: Glyndor/.github/.github/workflows/bun-ci.yml@c958978687af37dc2d826a967d9c549589afb39f # v1.14.1
    with:
      coverage-threshold: 90
