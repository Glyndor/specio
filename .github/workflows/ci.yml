name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  bun:
    uses: Glyndor/.github/.github/workflows/bun-ci.yml@fadfbebc989a58a2292438fecbd5a616ffa0bdf3 # v1.15.0
    with:
      coverage-threshold: 90
