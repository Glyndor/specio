name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  bun:
    uses: Glyndor/.github/.github/workflows/bun-ci.yml@8517d94fde00466f59b95c5d1bd838e2ad24a8bf # v1.15.2
    with:
      coverage-threshold: 90
